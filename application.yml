spring:
  profile:
    active:
      - dev
---
server:
  port: 7001
spring:
  profile: dev  # 开发模式
    application:
      name: dictservice-provider-user-dev

---
spring:
  profile:
    active:
      - test
---
spring:
  profile: test  # 测试模式
    application:
      name: dictservice-provider-user-test